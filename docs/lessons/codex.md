# Ratified lessons — Codex agents (Terra / Sol / Luna tiers)

Scope: lessons about **Codex agents' behavior or tooling** — how those
sessions run, their failure patterns, their tool mechanics. Domain, repo,
and process lessons belong in the shared ledger (`../lessons.md`). When in
doubt: shared.

Per the AGENTS.md tombstone, T/M/H/L are Codex *tiers*, not distinct
agents — one file covers them all.

**Owner-append-only.** Agents never write here; candidates go to
`proposed.md` and reach this file only through owner ratification. All
`../lessons.md` rules apply unchanged (actionable, ~6 lines, dedup before
append, supersede rather than rewrite). L-numbers come from the single
sequence shared with every ratified file; the master index in
`../lessons.md` lists every entry with its scope.

Conflict rule: where a scoped lesson contradicts a shared one, the shared
one wins until the owner rules; the reviewer flags the conflict rather
than either agent resolving it.

Codex sessions do not load `CLAUDE.md`; the standing brief template carries
the line instructing them to read `../lessons.md` and this file before work.

---

_No ratified entries yet. Existing Codex-specific lessons (e.g. L-0021
acting past a STOP) remain in `../lessons.md`; re-homing them is an
owner-batched consolidation decision, not automatic._
