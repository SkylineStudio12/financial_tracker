import { Gallery } from "./gallery";

/**
 * DEV-ONLY reference: the design-system component gallery.
 * Not linked from app navigation — visit /dev/components directly.
 * Static sample data only; nothing here touches the database.
 */
export default function ComponentsPage() {
  return <Gallery />;
}
